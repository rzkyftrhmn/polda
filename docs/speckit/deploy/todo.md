# Task: Deploy use Docker

## ✅ Tasks (Checklist)
- [x] jalankan specify.md
  - Created docker/ (Dockerfile, nginx.conf)
  - Added docker-compose.yml
  - Prepared .env for Docker
  - Pending: start Docker Desktop and run `docker compose up -d`